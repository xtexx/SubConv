name: Build

on:
  push:
    branches:
      - dev
    paths:
      - '.github/workflows/build.yml'
      - 'subconv/**'
      - 'mainpage/**'
      - 'template/**'
      - 'config.yaml.example'
      - 'api.py'
      - 'pyproject.toml'
      - 'uv.lock'
  workflow_call:
    

jobs:
  build:
    if: github.repository == 'SubConv/SubConv'
    strategy:
      matrix:
        os: [windows-latest, ubuntu-latest, macos-latest]
        arch: [x64]
        include:
          - os: macos-latest
            arch: arm64

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'
          architecture: ${{ matrix.arch }}

      - name: Setup uv
        uses: astral-sh/setup-uv@v4
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.11

      - name: Install Dependencies
        run: |
          uv sync --locked --group build

      - name: Build Executable
        run: |
          uv run python -m nuitka --onefile --assume-yes-for-downloads api.py

      - name: Build Frontend
        working-directory: mainpage
        run: |
          bun ci
          bun run build

      - name: Copy Files
        run: |
          mkdir output
          mkdir output/mainpage
          cp api${{ matrix.os == 'windows-latest' && '.exe' || '.bin' }} output/api${{ matrix.os == 'windows-latest' && '.exe' || '' }}
          cp config.yaml.example output/config.yaml.example
          cp -r template output/template
          cp -r mainpage/dist output/mainpage/dist

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: subconv-${{ matrix.os }}-${{ matrix.arch }}
          path: output/
